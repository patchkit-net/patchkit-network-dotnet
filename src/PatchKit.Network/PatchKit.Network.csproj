<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="12.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props" Condition="Exists('$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props')" />
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProjectGuid>{EE5FBFA6-CF9A-4393-B052-0757457FDFDB}</ProjectGuid>
    <ProjectTypeGuids>{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}</ProjectTypeGuids>
    <OutputType>Library</OutputType>
    <AppDesignerFolder>Properties</AppDesignerFolder>
    <RootNamespace>PatchKit.Network</RootNamespace>
    <AssemblyName>PatchKit.Network</AssemblyName>
    <TargetFrameworkVersion>v3.5</TargetFrameworkVersion>
    <FileAlignment>512</FileAlignment>
    <PackageLicenseUrl>https://raw.githubusercontent.com/patchkit-net/patchkit-network-dotnet/master/LICENSE</PackageLicenseUrl>
    <PackageProjectUrl>https://github.com/patchkit-net/patchkit-network-dotnet</PackageProjectUrl>
    <PackageReleaseNotes>https://github.com/patchkit-net/patchkit-network-dotnet/releases</PackageReleaseNotes>
    <PackageTags>patchkit network upsoft</PackageTags>
    <BeforePack>_RetrieveAssemblyVersion</BeforePack>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <PlatformTarget>AnyCPU</PlatformTarget>
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <PlatformTarget>AnyCPU</PlatformTarget>
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
    <DocumentationFile>bin\Release\PatchKit.Network.xml</DocumentationFile>
  </PropertyGroup>
  <ItemGroup>
    <Reference Include="System" />
    <Reference Include="System.Core" />
    <Reference Include="System.Data" />
    <Reference Include="System.Xml" />
  </ItemGroup>
  <ItemGroup>
    <Compile Include="DefaultHttpClient.cs" />
    <Compile Include="HttpGetRequest.cs" />
    <Compile Include="BytesRange.cs" />
    <Compile Include="HttpWebResponseAdapter.cs" />
    <Compile Include="IHttpClient.cs" />
    <Compile Include="IHttpResponse.cs" />
    <Compile Include="IRequestRetryStrategy.cs" />
    <Compile Include="IRequestTimeoutCalculator.cs" />
    <Compile Include="NoneRequestRetryStrategy.cs" />
    <Compile Include="Properties\AssemblyInfo.cs" />
    <Compile Include="Properties\VersionInfo.cs" />
    <Compile Include="SimpleInfiniteRequestRetryStrategy.cs" />
    <Compile Include="SimpleRequestTimeoutCalculator.cs" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="JetBrains.Annotations" Version="11.1.0">
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="NuGet.Build.Tasks.Pack" Version="4.5.0">
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
  <UsingTask TaskName="GetAssemblyPackageData" TaskFactory="CodeTaskFactory" AssemblyFile="$(MSBuildToolsPath)\Microsoft.Build.Tasks.v12.0.dll">
    <ParameterGroup>
      <AssemblyPath ParameterType="System.String" Required="true" />
      <PackageVersion ParameterType="System.String" Output="true" />
      <Authors ParameterType="System.String" Output="true" />
      <Description ParameterType="System.String" Output="true" />
      <Copyright ParameterType="System.String" Output="true" />
    </ParameterGroup>
    <Task>
      <Reference Include="System"/>
      <Reference Include="System.Reflection"/>
      <Using Namespace="System.Reflection"/>
      <Using Namespace="System.Diagnostics"/>
      <Code Type="Fragment" Language="cs">
<![CDATA[
var assembly = Assembly.LoadFrom(AssemblyPath);
var versionInfo = FileVersionInfo.GetVersionInfo(AssemblyPath);

string version = versionInfo.ProductVersion;

if(string.IsNullOrEmpty(version))
{
  version = assembly.GetName().Version.ToString();
}

PackageVersion = version;
Authors = versionInfo.CompanyName;
Description = versionInfo.FileDescription;
Copyright = versionInfo.LegalCopyright;
]]>
      </Code>
    </Task>
  </UsingTask>
  <Target Name="_RetrieveAssemblyVersion" DependsOnTargets="Build">
    <GetAssemblyPackageData AssemblyPath="$(OutputPath)/$(AssemblyName).dll">
      <Output PropertyName="PackageVersion" TaskParameter="PackageVersion"/>
      <Output PropertyName="Authors" TaskParameter="Authors"/>
      <Output PropertyName="Description" TaskParameter="Description"/>
      <Output PropertyName="Copyright" TaskParameter="Copyright"/>
    </GetAssemblyPackageData>
  </Target>
  <Target Name="LocalPublishPackage" DependsOnTargets="Pack" Condition=" '$(Configuration)' == 'Debug' ">
    <Exec Command="nuget add $(PackageOutputAbsolutePath)$(PackageId).$(PackageVersion).nupkg -Source ~/.nuget_local" />
  </Target>
  <Target Name="PublishPackage" DependsOnTargets="Pack" Condition=" '$(Configuration)' == 'Release' ">
    <Exec Command="nuget push $(PackageOutputAbsolutePath)$(PackageId).$(PackageVersion).nupkg -Source https://api.nuget.org/v3/index.json" />
  </Target>
  <!-- To modify your build process, add your task inside one of the targets below and uncomment it. 
       Other similar extension points exist, see Microsoft.Common.targets.
  <Target Name="BeforeBuild">
  </Target>
  <Target Name="AfterBuild">
  </Target>
  -->
</Project>